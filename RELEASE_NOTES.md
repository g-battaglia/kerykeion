# Release Notes

The full, current change history lives in **[CHANGELOG.md](CHANGELOG.md)**.

Longer per-version notes are kept under [`release_notes/`](release_notes/) for the
releases that warrant them. The directory is selective, not exhaustive — some
version bumps carry no note. `CHANGELOG.md` is the complete record.
