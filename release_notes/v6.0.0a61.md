# v6.0.0a61

This alpha bumps the `libephemeris` pin to `3.0.0rc1` and lands two documentation
passes.

## Dependency

The `libephemeris` pin moves to `3.0.0rc1`.

## Documentation

- A completeness, accuracy and navigation pass across the `README` and
  `site/docs`.
- A second deep-review pass that expands the factory documentation and fixes
  regressions introduced by the first pass.
