# v6.0.0a85 — The whole library, from the terminal

This alpha adds an **optional** command-line interface. **No calculation changed,
and no public name moved:** `kerykeion.__all__` is exactly what a84 exported,
every Pydantic schema is byte-identical, and `import kerykeion` is unchanged. The
CLI is an `extra` — `pip install kerykeion` without it behaves exactly as before.

## `kerykeion[cli]`

A new optional extra exposes the entire library from the terminal, without
writing any Python:

```
pip3 install "kerykeion[cli]"
```

It is a **local** engine: it computes with the same in-process backend as the
library (libephemeris by default, Swiss Ephemeris when installed), so it works
offline and needs no API key. It is backend-agnostic — every command produces the
same output on either backend, exactly as the library does.

### What you can do

- **Charts:** `natal`, `synastry`, `transit`, `composite`, `return`,
  `progression`, and `now` (the current moment).
- **Analytical techniques:** `technique` groups profections, firdaria, zodiacal
  releasing, mutual receptions, horary, midpoints, directions, astro-cartography,
  fixed stars and relocation.
- **Sky events:** `sky` groups eclipses, lunations, sign ingresses, retrograde
  stations, mundane aspects, planetary phenomena, nodes, sun times, planetary
  hours and void-of-course moon.
- **Time series:** `ephemeris` and `transits` sample positions over a range.
- **Subject profiles:** `subject save|show|list|path|verify` — save a birth-data
  recipe once (`0600`, because birth data is PII) and reuse it everywhere with
  `-s <name>`.
- **`call`:** a guarded dispatcher that reaches any public `Factory.method` (or
  bare function) in `kerykeion.__all__` without a dedicated command.

### Output discipline

The output format is chosen for you: **text on a TTY, JSON in a pipe**. So
`kerykeion natal -s ada` reads as a report, while
`kerykeion natal -s ada | jq -r .sun.sign` works with no extra flag. `-f` forces
`text|json|xml|svg`; `-o file` infers the format from the suffix.

Two invariants hold everywhere:

1. **Payloads go to stdout, warnings go to stderr.** A payload piped to `jq` is
   never corrupted by an ephemeris-coverage notice, even under `-f json`.
   `--warnings-as-errors` turns them into exit `9` *after* the payload prints.
2. **No `rich.Console.print` on stdout.** Rich would wrap a fixed-width report
   and inject ANSI into a piped JSON. Rich is used only for chrome (help, error
   panels), on stderr.

### Clean errors, never a traceback

Every error is one line on stderr with the right code, never a traceback (use
`--traceback` to see one):

| Code | Meaning |
|------|---------|
| `0` | success |
| `1` | unexpected error |
| `2` | usage error (Click/Typer) |
| `3` | a full-CLI subcommand needs the `[cli]` extra (the base command — `status`, `--version`, `--help` — works without it) |
| `4` | invalid input |
| `5` | a `KerykeionException` |
| `6` | an ephemeris / backend error |
| `7` | a network error |
| `8` | sampling limit exceeded |
| `9` | warnings treated as errors |
| `130` | interrupted |

## The import invariant, and a useful command without the extra

`import kerykeion` stays free of `typer`. The `kerykeion` command is installed
even by a plain `pip install kerykeion` (the console script is static metadata),
so the no-extra path is the default for users who do not read the docs. Rather
than a dead end, the bare command is **useful out of the box** through a
stdlib-only core that needs no `typer`/`rich`:

- `kerykeion status` prints the runtime state — active backend (libephemeris or
  Swiss Ephemeris) and its version, the LEB calc mode, the precision tier and
  network policy, the LEB data files (paths, sizes, year coverage, body count),
  whether pyswisseph is available, and the `KERYKEION_*` / `LIBEPHEMERIS_*`
  environment knobs in effect. `--json` emits the same as machine-readable JSON.
- `kerykeion --version` / `-V` and `kerykeion --help` / `-h` (and a bare
  `kerykeion`) exit `0`.

`--version` and `--help` do not even import `kerykeion` (the version is read
via `importlib.metadata`), so they return instantly; only `status` pays the
backend init, and it does so to report real state. Any *other* subcommand
(`natal`, `transit`, …) prints a one-line install hint and exits `3` — never a
`Traceback`.

This is enforced in three places: `kerykeion/cli/__init__.py` imports `typer`
only lazily inside `main()`; `kerykeion/__init__.py` never imports `kerykeion.cli`
(it is untouched); and `tests/core/test_public_api_surface.py` excludes
`kerykeion.cli` from its `walk_packages` import sweep. A dedicated test asserts
that `import kerykeion` leaves `typer` out of `sys.modules`, and a subprocess
test drives the no-extra dispatch by blocking `typer`/`rich` on `sys.meta_path`.

## Security is an allowlist

`kerykeion call` reaches any name in `kerykeion.__all__`, split on a single `.`,
with no private members and no activated descriptors. `kerykeion call os.system`
is refused — `os` is not in `__all__`:

```
$ kerykeion call os.system --param cmd=ls
kerykeion: error: 'os' is not in the kerykeion public API; ...
```

That refusal is an explicit test, not a hope.

## Testing for identity, not snapshots

`tests/core/test_cli.py` asserts that a command's payload **equals the library
model built from the same inputs** — `json.loads(stdout) ==
json.loads(model.model_dump_json())` — never a golden file. An identity test is
immune to ephemeris drift: if the Sun moves, both sides move together. There is
deliberately **no** `regenerate:cli` task. The suite carries the `cli` marker
(`poe test:cli`) and runs in-process via `typer.testing.CliRunner`.

## Dependencies and licensing

The extra adds `typer` and `rich` — both **MIT**. No Apache-2.0 dependency was
added (the repo's standing rule); `NOTICE` carries a new "Optional command-line
interface" block listing every transitive license (typer, rich, shellingham ISC,
annotated-doc, markdown-it-py, mdurl, pygments BSD-2, colorama BSD-3; Click is
vendored by Typer under its MIT, not listed separately).

`typer` and `rich` are declared in **both** the `[cli]` extra and the `[dev]`
group. This is intentional, not redundancy: `pyrightconfig.json` runs in `basic`
mode where `reportMissingImports` is an error across the whole `kerykeion/` tree,
so without them in the dev environment every contributor's `poe typecheck` fails
on `kerykeion/cli/`. The extra is what a user installs; the dev group is what a
contributor needs to type-check.

The local-gates-only policy is unchanged: no CI was added, and
`.github/workflows/` stays empty. The CLI ships two new local tasks —
`poe test:cli` and `poe cli:smoke` — and `poe build:smoke` grew a third isolated
environment that verifies the no-extra degradation (the command exists, exits
non-zero with the install hint, and prints no traceback).

## Hardened after a review pass

Before merge the CLI was put through a code review that verified each finding
against the live interpreter. Every silent-wrong-output and exit-code path it
reached was closed (the CHANGELOG carries the full list); the headline fixes:

- **A date-only natal is no longer silently non-deterministic.** `natal` without
  `--time` now requires it (exit 4) — the factory was filling the birth time from
  the wall clock, so two runs a minute apart produced different charts.
- **`--no-online` / `--offline` actually stay offline**, overriding a profile
  saved with `online=True`, and apply to the natal half of a transit/return dual
  wheel — not just the transit moment.
- **`--set` validates against the recipe shape**, not the raw factory signature,
  so factory-only keys (`year`, `hour`, `suppress_geonames_warning`, …) are
  rejected instead of colliding in `materialize` or breaking `subject save`.
- **The ephemeris pre-flight count is DST-aware** (UTC for hours/minutes), so it
  matches the library's own ceiling check across a DST boundary instead of
  under-counting and letting an over-ceiling series slip to the wrong exit code.
- **Every bad path reaches the right exit code** — `-o` on a directory, `--step 0`,
  `--year 0`, `--time 24:00`, `transit --to-time` without `--to-date`, a typoed
  `call --param`, and a `typer.Exit` through the boundary are all the intended
  clean code (mostly exit 4), never exit 1 with a traceback.

A **second** review pass (same method: every candidate verified against the live
interpreter) closed a further round of gaps, with one regression test per fix:

- `call -s` now binds a `Union`-typed subject parameter (`AstrologicalSubjectModel |
  CompositeSubjectModel | PlanetReturnModel`, as on every `AspectsFactory` method),
  which `_is_subject` had misclassified — so the whole aspects surface was
  unreachable through `call -s`.
- `transit -s <profile>` defaults the transit moment to the **natal birthplace**
  (offline) instead of going online with an empty GeoNames query; `--to-date`
  without `--to-time` is a clean exit 4 (not a misleading "use `kerykeion now`").
- `--set active_points=sun,moon` coerces to a list (it stored a string the recipe
  then rejected); `sky eclipses` with only one of `--lat`/`--lng` is rejected
  rather than silently running a global search.
- `call --list` no longer advertises Pydantic-model methods it cannot dispatch;
  `--warnings-as-errors` is honoured even when the renderer itself crashes.
- `-o` files and saved profiles are UTF-8 with **LF** endings on every platform
  (no cp1252 `UnicodeEncodeError`, no CRLF corruption of JSON/SVG), and
  `created_at` is UTC-aware.
- libephemeris coverage/data errors map to exit 6 (not 5/4); `transits` surfaces
  a missing profile as exit 4 instead of swallowing it; `main(argv)` honours an
  explicit `argv` on the Typer path too.

Three findings were deliberately left out of this pass: `**kwargs` dispatch in
`call` (no public method currently has one — the guard already fails safe);
Click in the base `NOTICE` (pre-existing, unrelated to this feature); and gating
`test:core` on the `[cli]` extra (the dev workflow always installs it, and an
`importorskip` would hide regressions — a stated principle of this suite).

A **third** review pass (same method) closed a further round, again with one
regression test per fix. The two highest-severity items were silent wrong-output
bugs in the hand-maintained house-system name map, caught by cross-referencing
it against the library's `HousesSystemIdentifier` Literal:

- **`--houses porphyry` no longer silently builds Alcabitius.** The map coded
  Porphyry as `"B"` (Alcabitius); the correct code is `"O"`. **`--houses apc`
  no longer always fails:** it was coded `"n"`, which is not even a member of
  the Literal (APC is `"Y"`). An unknown single letter (`G`, `J`, …) is now
  rejected at the flag with a clear "unknown house-system letter" message
  instead of a confusing pydantic literal error from the factory.
- **`technique directions` no longer advertises a `--planets` flag that always
  crashed.** Primary directions have no planet filter; the flag was bound to the
  factory's `aspects` parameter (which takes *angles*), so any value raised. It
  is now `--aspects`, validated against the real aspect angles.
- **The transit wheel now shares the natal frame.** `transit` carried only the
  coordinates into the transit subject, so the outer ring was always
  Tropical/Placidus regardless of a Sidereal natal; `create_transit_chart_data`
  does not re-frame, so both rings must be built in the same zodiac.
- **An offset-bearing `sky --from` is honoured.** `sky voc|hours|sun-times`
  extracted the raw year/month/day/hour/minute from `--from` and re-read them in
  `--tz`, so `--from …T12:30:00Z --tz Europe/Rome` shifted the moment by two
  hours; an offset-aware instant is now converted into `--tz` first.
- **`--refine` without `--events` is rejected** (it only sharpens events and was
  a silent no-op otherwise); **a typoed `call Factory.method` is exit 4**, not
  exit 1 with a traceback (a missing member raised `AttributeError`, which
  `classify` did not list); **a dict `--param` is parsed as JSON** (it was
  forwarded as a literal string, so `custom_weights='{"sun":1.5}'` could never
  reach the factory).
- **Warnings reach the right stream and the right commands.** `emit_warnings`
  now resolves `sys.stderr` at call time (the default arg bound it at import, so
  CliRunner/capsys redirection missed it); `subject show|list|verify` route
  through the unified warnings funnel, and `verify` collects warnings from the
  materialised subject (not the compact summary it prints), so
  `--warnings-as-errors` engages there too.
- **`-o` into a new directory works** (the parent is created, matching
  `subject save`); **a `-s` lookup miss no longer creates the profile store** as
  a side effect of gathering suggestions; and the CLI's `--traceback` /
  `--warnings-as-errors` knobs are reset between in-process test runs (an
  autouse fixture) so the suite is no longer order-dependent.

Two findings were deliberately left out of this pass: the sampling pre-flight's
off-by-one across a DST boundary (#10 — the library's own guard is authoritative
and fires anyway; mirroring its `localize_naive` internals would be fragile
coupling), and the `**kwargs` half of the `call --param` structural-param gap
(no public method has `**kwargs`, so the guard already fails safe).

> Both of those parentheticals were later overturned — the DST deferral by the
> fourth pass, and the claim that the dict/JSON case was "the only reachable one"
> by the fifth: `Sequence[str]` parameters were reachable *and* broken. See below.

A **fourth** review pass (extra-high effort) closed a further fifteen gaps, each
pinned by a regression test in `TestXhighReviewFixes`. Its largest cluster
**overturns a deferral from the third pass**: the sampling pre-flight now agrees
with the library to the sample.

- **The pre-flight sample count now matches `EphemerisDataFactory` exactly.** The
  third pass deferred the DST-aware pre-flight, reasoning that mirroring the
  library's fold resolution would be fragile coupling to internals. That was
  wrong: `localize_naive` is a **public** helper in `kerykeion.utilities`, and the
  factory itself calls it. The pre-flight now uses it (with `is_dst=False`, the
  factory's own default) for the hours/minutes UTC bounds, and converts aware
  bounds to local-naive before counting days — exactly what
  `EphemerisDataFactory` does. Two regression tests assert
  `count_samples(...) == len(EphemerisDataFactory(...).dates_list)` across a DST
  fall-back fold and a spring-forward day boundary, so the dedicated exit 8 fires
  when the library's own ceiling would, and no longer under- or over-counts into a
  mid-run crash.
- **`transits` is DST-aware for online/city profiles too.** It read the natal zone
  from the stored *recipe* (`input.tz_str`, `None` for an online/city profile —
  the zone is resolved by GeoNames only at materialisation); the natal is now
  materialised before the pre-flight so its *resolved* zone drives the count.
- **`--no-limit` no longer skips range validation.** It gates the sample
  *ceiling*, not the inverted-range or mixed-awareness check, so
  `--from 2025-12-31 --to 2025-01-01 --no-limit` now gives the clean "--to must
  not precede --from" instead of a generic mid-run error. Mixed offset-aware/naive
  `--from`/`--to` raise a clean "same ISO form" error instead of Python's
  `TypeError: can't compare offset-naive and offset-aware datetimes`.

Four **silent-wrong-output** bugs:

- **A relocated `transit`/`return` no longer silently uses the natal timezone.**
  Overriding `--lat`/`--lng` without `--tz` built the chart at the new coordinates
  but the natal zone — a multi-hour UTC error in the houses/Ascendant. A partial
  relocated-location group is now a clean exit 4 (give all three, or none).
- **Inline `--lat`/`--lng`/`--tz` now override the profile in `sky`** (universal
  CLI convention: an explicit value wins), where the profile's coordinates
  previously won and the inline flags were ignored.
- **`sky voc --from … --to …` now honours `--tz`/`-s`.** `from_iso_range` is
  UTC-only, so naive bounds were read as UTC; the zone's offset is now attached to
  naive bounds so the library converts them to the intended UTC instants.
- **`--warnings-as-errors` is no longer bypassed for relationship scores.** The
  warning collector recursed the singular `subject`/`first_subject`/
  `second_subject` but not the plural `subjects` list on
  `RelationshipScoreModel`; it now does.

Five correctness/consistency fixes: **`--zodiac tropical`/`SIDEREAL` now work**
(case-insensitive, matching `normalize_zodiac_type`; the CLI was stricter than the
library it drives); **a non-string `Literal` parameter** (`Literal[1, 2]`) coerces
by value *and* type, so `--param x=1` reaches it as `1` (and `True`/`1` do not
collide); **PEP 604 `X | Y` unions** classify and coerce like `typing.Union` (the
first public method to adopt the modern syntax would have broken `call -s`); **an
aware `--from` on a DST fall-back's second reading** is surfaced as an error (the
moment factories take wall-clock components without fold, so it could not be
carried through without a one-hour guess); **an explicit `--seconds 0` is
forwarded** (`if seconds:` truthiness dropped it); and **`--param x=none`/`null`
maps to `None`**, matching the `--set` path.

The xhigh pass also **refuted and dropped** two of its own candidate findings,
worth naming because they would have been busywork: the `**kwargs` dispatch claim
in `call` (no public method has `**kwargs`; `**subject_kwargs` is on `__init__`,
which the registry already filters), and the `_now_iso` UTC "discontinuity" (a
deliberate, documented improvement, not a regression).

## A fifth pass, over the whole PR

The passes above reviewed each change as it landed. A final extra-high-effort
pass reviewed the **finished feature end to end** (~7.5k lines) and found
fifteen more, each now pinned by a test in `TestFullPrReviewFixes`.

Three were **user-visible bugs**:

- **`call` could not pass a `Sequence[str]` parameter.** Coercion handled
  `list`/`set`/`frozenset`/`tuple` but not the abstract `Sequence` origin, so the
  raw string reached the factory and it refused: "must be a sequence of point
  names, not a single string". Nine parameters across `MidpointFactory`,
  `SolarArcFactory`, `SecondaryProgressionFactory` and `HeliacalFactory` were
  affected, and the JSON escape hatch `--explain` advertised did not work either
  (they were mislabelled `json-only`). They now coerce exactly like a list —
  `--param active_points=Sun,Moon` works — and `--explain` says `cli`.
- **House-system letters are case-significant, and the CLI was flattening them.**
  `i` (Sunshine/alt.) and `I` (Sunshine) are two different systems, but every
  single letter was upper-cased: `--houses i` silently built `I`, so `i` was
  unreachable from the CLI, and a `transit` inheriting a natal cast with `i` cast
  its outer ring with `I` — the exact frame divergence that inheritance block
  exists to prevent. A letter that is already valid is now kept as typed; the
  upper-casing convenience survives for the unambiguous ones.
- **`--no-online` did not exist.** The resolver implemented it and the notes above
  documented it, but the option was declared `"--online"` with no secondary flag,
  so typer never generated `--no-online` and the branch was dead. It is now
  `"--online/--no-online"`.

Two were **data-safety** fixes on the profile store, which holds birth data:

- **`subject save` is now atomic.** It truncated the existing profile before
  writing the new one, so an interruption (full disk, Ctrl-C, a serialisation
  error) left a zero-byte or half-written recipe and the birth data was gone.
  The write goes to a temp file in the same directory and is `os.replace`d into
  place — either the old profile or the new one, never a fragment.
- **The app directory is 0700 too.** `ensure_profile_store()` promised 0700 on
  both directories but had no callers; `profiles.save` re-implemented half of it
  inline and only chmodded the `subjects/` subdir, leaving `~/.config/kerykeion`
  world-readable. `save` now calls the shared helper.

One was a **licensing statement**, the class of error this repo has been bitten
by twice: NOTICE claimed Typer's vendored Click is "carried transitively under
Typer's MIT". Bundling does not relicense: the vendored copy stays BSD-3-Clause
(verified at `typer/_click/LICENSE.txt` in the installed distribution). Reworded.

The rest were correctness-adjacent cleanups: bare `@app.command` (no parentheses)
silently registered nothing and rebound the decorated name — now fixed; the
enum-style flags (`--lot`, `--rate`, `--method`, `--type`) were case-sensitive
while `--zodiac`/`--houses`/`--points` were not, so `--lot Fortune` failed —
now one rule; `sky` delegates `--zodiac` to the library's own
`normalize_zodiac_type` instead of re-implementing its spelling table; `sky` no
longer builds a full ephemeris subject only to discard it when every coordinate
was given inline; `technique stars` uses the shared `CountOpt`; the `all` extra
self-references `kerykeion[cli,swiss]` instead of restating their version floors
a third time; and a family of `emit_*` helpers that wrote straight to stdout —
bypassing `-o` and the warnings funnel — was deleted along with two other dead
symbols, leaving `render` + `write_output` as the single funnel.

Two docstrings were also corrected rather than defended: `kerykeion.cli` claimed
`--version`/`--help` avoid the ~1.5 s backend init. They cannot — the entry point
is `kerykeion.cli:main`, and importing a submodule imports its parent package
first, so `kerykeion/__init__.py` always runs (measured: 1.3 s). The **typer**
isolation that module does buy is real and gated by a test; the speed claim was
not, and making it true is a separate change to `kerykeion/__init__.py` (PEP 562).

## Post-review internal cleanup

After the review passes settled, a small cleanup folded the duplication the CLI
had accreted — strictly internal, no calculation or output changed:

- **One home for the shared command helpers.** `_emit`, `_split_csv` and
  `_parse_dt` were copied verbatim into `call`, `charts`, `technique`, `sky` and
  `series`; they now live in a single leaf module, `cli/commands/_shared.py`,
  imported by all five. (The leaf imports only the `warnings`/`formats` helpers and
  the stdlib, so it can never cycle.) `charts._emit_subject_or_chart` stays put —
  unlike `_emit` it branches on SVG, so it is not the same function.
- **`_STEP_TYPES` is no longer a second source of truth.** It restated the values
  of the `StepType = Literal["days", "hours", "minutes"]` the sampler already owns;
  it is now `get_args(StepType)`, so the two cannot drift.
- **`registry.public_names()` is cached and read-only.** It walked
  `kerykeion.__all__` and re-filtered on every call; it is now built once
  (`functools.cache`) and returned as a `types.MappingProxyType`. Caching makes
  every caller (`resolve_target`, `call --list`, `--explain`) share one object, so
  the map is immutable on purpose — a mutation cannot leak across the process even
  if a future caller tries. (A regression test pins the immutability.)
- **`subject_resolver`'s signature cache converges to the same pattern.** The
  hand-rolled `_FACTORY_PARAMS` global is gone, replaced by `_factory_params(mode)`
  — `@functools.cache`, returning an immutable `frozenset`. (Its predecessor's
  `dict.setdefault` evaluated the signature on every call — the default is eager —
  so the cache it appeared to keep never saved the work. Correctness was never
  affected; this is a consistency + efficiency fix.)
- **Dead `cli/context.py` removed.** Its `was_given()` and `kk()` had zero callers
  (the resolver relies on the `None`-default convention directly), and the only
  trace left was a stale `options.py` docstring pointing at `was_given`, now fixed.
- **The consolidation was finished.** `subject show`/`list` now use the shared
  `_emit` too (only `verify`, which passes a `warning_source=`, stays inline); the
  ceremony-only `_emit as _emit_chart` alias in `charts` was dropped (it visually
  collided with the genuinely-different `_emit_subject_or_chart`); and the `--from`
  help text now matches the `_parse_dt` error wording.

## Completing the coverage

A measured audit of the finished CLI — running it, not reading it — found the
**computation** fully covered: every public factory method reachable, either from
a curated command or from `call`, with no parameter classified unsupported. The
**presentation** was another matter, and this is what closes it.

**`ChartDrawer`'s twenty parameters were unreachable.** `svg_out` built it as
`ChartDrawer(obj)` — defaults only — and there was no fallback: every one of its
methods needs a `chart_data` model that `--explain` marks `json-only` and `-s`
cannot bind. Chart appearance was the single part of the library the terminal
could not touch. All of it is now flags: six themes, ten chart languages, two
styles, the layout and visibility toggles, `--svg-variant full|wheel|aspect-grid`
reaching the two drawer methods the CLI never called, and `--chart-settings
file.json` for the structural tables. Mapping sections merge over the library's
defaults, so overriding one colour no longer means restating a 40-entry palette —
the naive version replaced it wholesale and the drawer died on
`KeyError('zodiac_icon_0')`.

The cause of the gap was structural, not an oversight: rendering was
`render(obj, fmt)`, with nowhere to put an option. A frozen `RenderOptions` now
threads the funnel as a trailing `opts=None`, which is also why
`--no-aspects`/`--max-aspects` and `--envelope` could finally be wired.

**Ten more curated commands** bring every public factory to a real `--help`:
`aspects`, `dominants`, `moon`, `relationship-score`, `technique
house-comparison`, `technique solar-arc`, `technique fixed-stars`, `sky mundane`,
`sky phenomena`, `sky occultations`. 34 commands became 50. `call` remains the
universal valve, not the only route.

**The CLI can describe itself.** It validated against 23 house systems, 48
ayanamsas, 11 perspectives and 76 points while being unable to list any of them.
`info literals|points|stars|houses|methods` reads those tables from the library at
runtime — never transcribed — and with `-f json` it is the source a script or an
agent should consult instead of a hard-coded copy. `doctor` completes the pair:
the environment probes `status` already ran, plus a real natal calculation, plus
a verdict and exit 6 when the install is genuinely broken.

**`--snapshot` was a stub in three parts** — documented in `profiles.py`, present
in the schema, written and read by nothing. It now caches the computed subject
and is dropped automatically when the kerykeion version or ephemeris backend
changes, because reusing numbers this installation would not compute is worse
than the work it saves. `subject verify` recomputes on purpose and reports
absent / matches / stale / **drifted**.

Two defects worth naming, both found by making a checker pass rather than by a
happy-path run: mypy caught that the declination aspect variants take
`(subject, active_points, orb)` and would have raised `TypeError` on the
`active_aspects` the command was forwarding; and the CLI skill's new bash gate
caught a documented `sky sun-times` example missing its required `--from`.

The **`kerykeion-cli` agent skill** ships alongside, with
`poe skill:cli:smoke` executing all forty of its shell examples in a sandboxed
profile store — because a skill is copied verbatim into other people's
repositories, where a command that does not exist is confident, unfalsifiable
instruction.

## Compatibility

Requires `libephemeris>=3.1.0,<4` and Python 3.12+, unchanged from a84. The
`[cli]` extra adds `typer>=0.26.4` (which vendors Click, so the CLI does not
depend on whichever `click` another dependency resolves) and `rich>=13.8.0`.
